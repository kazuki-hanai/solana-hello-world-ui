import React from 'react';

export const Description = (): JSX.Element => {
    return (
        <p className="text-black bg-white p-4 rounded mb-4">
            Tailwind, with built-in tooling for custom configs
        </p>
    );
};
